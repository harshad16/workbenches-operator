# OpenShift-specific overlay. Requires OpenShift's service-CA controller.
# For vanilla Kubernetes clusters, use config/certmanager/ instead.
resources:
- ../base

patches:
- path: webhook_openshift_patch.yaml
- path: webhookconfig_openshift_patch.yaml
